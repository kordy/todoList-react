import taskConstants from '../constants/taskConstants';

const baseState = {
  taskList: [],
  newTask: '',
};

function selfieList(state = baseState, action) {
  return true;
}

export default selfieList;
